# DAILY_LOG — 2026-08-10 (개발 시작 진단, 코드 미수정)

이 문서는 STEP 작업 기록이 아니라 **오늘 개발을 이어가기 전 상태 진단**이다. 코드/설정/데이터는
전혀 수정하지 않았다. 근거는 전부 `code-review-graph` MCP, `git log`/`git diff`/파일 mtime,
`docs/`, `work/reports/`, 실제 `node --test` 실행 결과에서만 가져왔다. 추측한 항목은
"확인 필요"로 명시한다.

---

## 0. 핵심 발견 — docs/와 실제 작업 이력(work/reports/) 사이의 심각한 괴리

`docs/ROADMAP.md`(마지막 갱신 표기: 2026-08-08)는 "STEP66-2에서 세션 종료, 다음 세션 첫 작업은
BRAND INTELLIGENCE DATA COMPLETION 고정"이라고 기록하고 있다. 그러나 **실제로는 그 다음 작업이
이미 STEP67-1부터 STEP67-8D까지 진행되어 있었다** — 단, 전부 `work/reports/STEP67-*.md`
(6개 파일, mtime 2026-08-08 15:45 ~ 2026-08-10 01:20)에만 기록되어 있고, `docs/ROADMAP.md`/
`docs/PROJECT_MEMORY.md`/`docs/DECISIONS.md`/`docs/DAILY_LOG/`는 전혀 갱신되지 않았다.

**원인(확인됨)**: `work/`는 `.gitignore`에 `work/` 한 줄로 전체 무시되고 있다(`.gitignore` 6번째
섹션). 즉 `work/reports/STEP67-*.md`는 git 이력에 절대 남지 않고, `docs/`가 수동으로 갱신되지
않으면 다음 세션은 이 파일들을 열어보지 않는 한 STEP67 시리즈 전체를 알 수 없다. 이번 진단은
`work/reports/`를 직접 읽어 이 괴리를 발견했다.

**영향**: `docs/ROADMAP_BACKLOG.md`의 BACKLOG-004("Monthly Report/Resolver F는 의도적으로
범위 밖")도 이미 사실과 다르다 — 아래 6번 항목 참고.

---

## 1. 2026-08-09까지 실제로 완료된 개발 STEP

### git에 이미 커밋된 부분 (HEAD `4f6f827`, 2026-08-05 15:06:52 +0900)

커밋 메시지 기준(최근 5개, 최신순):
- `4f6f827 feat(work-data): support master data uploads` (08-05 15:06)
- `79c4302 feat(monthly): automate offline sales refresh workflow` (08-05 13:53)
- `84791b8 feat(master-data): build brand sourcing and integrity workflow` (08-03 02:30)
- `4ec190d fix(clients): align Cafe24 order source with commerce` (08-01 22:48)
- `39c67bd fix(annual): reflect canonical brand top5 in annual archive` (07-30 15:05)

Master Data Phase 1(승인 워크플로/merge plan/브랜드 소싱 검토)의 **핵심 골격은 이미 git에
커밋되어 있다.**

### docs/에는 기록됐지만 git에는 없는 부분 (STEP63 시리즈, `docs/ROADMAP.md`/`PROJECT_MEMORY.md`)

Identity Pipeline 설계·Clients/Brand Dashboard/Commerce 마이그레이션(STEP63-0~63-6), Promotion
Intelligence 신규 구축(STEP64-4~65-6), Design System 재설계(STEP66-0~66-2) — 전부 uncommitted
working tree 변경 안에 섞여 있다(`work/reports/DAILY_CLOSEOUT_2026-08-08.md` 참고, mtime
08-08 04:25).

### docs/에도 work/reports/에도 있는, 완전히 undocumented 후속 시리즈 (STEP67-x)

이번 진단에서 **최초로 발견**(work/reports/ 직접 열람):

| STEP | 파일 | 결과 | 핵심 내용 |
|---|---|---|---|
| STEP67-6 | STEP67-6.md | **WARNING** | Brand Intelligence 6개 미연결 항목 중 SKU/Customer Composition/AI Insight 3개를 실데이터 연결(새 Resolver 없이 기존 자산 재사용). Sell-through/Inventory/Recommended Action은 canonical source 자체가 없어 정직하게 SOURCE NOT AVAILABLE 표시. CARNET ARCHIVE/KIMYO/TROUBLED WATERS 3개 브랜드 Chrome 실측 검증 완료. 회귀 테스트 34/34 PASS. |
| STEP67-7 | STEP67-7.md | **PARTIAL/WARNING** | Canonical Brand Selector 표시명 정리, Inventory를 exact canonical 매칭으로 연결. **매출 재확인 중 온라인 510,400원 불일치 발견(FAIL로 기록)** — STEP67-7 자체가 매출 계산을 바꾼 게 아니라 조회 시점 값 차이로 분리해 기록. Chrome 세션 사용 불가로 QA 미수행. |
| STEP67-7B | STEP67-7B.md | **WARNING** | Placeholder 완전 제거(가짜 고객/SKU/카테고리 예시 0건). 245/245 테스트 PASS. Chrome 여전히 사용 불가. |
| STEP67-8 | STEP67-8.md | **PARTIAL** | Category 공식 taxonomy API가 401이라 매핑 불가(BLOCKED로 정직 표시), System Status를 `/api/status` 실데이터에 연결. Chrome 여전히 BLOCKED. |
| STEP67-8A | STEP67-8A-UI-AUDIT.md | 감사 전용 | 57개 UI 요소 전수 감사 — 28개가 "복구 필요"(Compare 모드 전체, Health Score 시각, Category 드릴다운, 고객 상세 등이 숨김/제거된 상태). |
| STEP67-8B | STEP67-8B-UI-RESTORE.md | **READY_FOR_USER_QA** | 28개 전부 정적으로 복구(가짜 값 재도입 0건). 246/246 테스트 PASS. **"코드/정적 검증은 끝났지만 사용자가 Chrome에서 확인하기 전까지 PASS로 표기할 수 없다"고 스스로 명시.** |
| STEP67-8C | STEP67-8C-FINAL-UI-VERIFICATION.md | 재검증 | STEP67-8B의 PRESENT_SAME 판정을 승계하지 않고 재분류 → 7개 항목이 여전히 1:1 불일치(Compare 도넛/Trend 점선/Category 카드/드릴다운 등). |
| STEP67-8D | STEP67-8D-LOCKED-UI-RESTORE.md | 정적 PASS | 위 7개 전부 구조적으로 복구. 246/246 테스트 PASS. **"Browser/pixel verification: USER CHROME QA REQUIRED"로 마감.** |

**결론**: 2026-08-09까지(그리고 08-10 새벽까지) Brand Intelligence Data Completion은 이미
**정적 검증(코드/테스트) 기준으로는 완료**됐다. 그러나 STEP67-7부터 STEP67-8D까지 6개 보고서
전부가 "Chrome 사용 불가/USER QA REQUIRED"로 마감돼, **단 한 번도 사용자 브라우저 확인을 받지
못한 채 계속 다음 하위 STEP으로 넘어간 상태**다.

---

## 2. 현재 진행 중이던 STEP (중단 지점)

**STEP67-8D — Brand Selector Flicker 조사 및 최소 수정**(가장 최신 파일 2개, mtime 08-10 01:03/01:20).

1. `STEP67-8D-BRAND-SELECTOR-FLICKER-ROOT-CAUSE.md`: 사용자가 Chrome QA 중 "브랜드를 바꾸면
   selector 영역이 순간적으로 사라졌다 나타난다"를 보고 → MutationObserver+Layout Instability
   API로 3단계 실측 조사. DOM 제거/재생성 없음, display/visibility/opacity/class 변경 없음을
   확인. selector trigger의 `width`가 브랜드명 길이에 따라 애니메이션 없이 즉시 스냅되는 것만
   실측 확인(코드 수정 없음, 보고서만 생성).
2. `STEP67-8D-BRAND-SELECTOR-FLICKER-MINIMUM-FIX.md`: 위 조사에 근거해 **`outputs/samplas-
   marketing-os.css`의 `.brand-selector-trigger`에 `min-width: 206.5625px;` 단 한 줄만 추가**
   (실측된 최대 관찰 폭 그대로 사용). HTML/JS/테스트는 전혀 건드리지 않음. `node --check` PASS,
   `git diff --check` PASS. **"Chrome QA Required: YES — 이번 수정이 실제로 flicker를
   개선했는지 아직 확인하지 않았다"로 명시적으로 마감.**

이 CSS 1줄 수정이 **working tree의 가장 마지막 코드 변경**이다(rebuild된 code-review-graph의
`last_updated: 2026-08-10T01:20:34`도 이 시각과 거의 정확히 일치 — 그래프가 이 수정 직후
재빌드됐다는 뜻).

**즉 지금 이 세션은 "STEP67-8D의 CSS 수정이 실제 Chrome에서 flicker를 없앴는지 확인되지 않은
채로" 중단된 지점에서 이어가는 것이다.**

---

## 3. 현재 working tree 변경의 기능 단위 분류

| 파일/그룹 | 분류 | 근거 |
|---|---|---|
| `outputs/samplas-marketing-os.{html,js,css}`, `server.mjs`, `intelligence-service.mjs` | **완료됐지만 미커밋** (정적 검증 기준) / Chrome 최종 확인 대기 | STEP64~STEP67-8D 전체가 이 5개 파일에 누적. 246/246 테스트 통과(직접 재실행 확인, 아래 7번). `git diff --check` clean. |
| `scripts/monthly-brand-sales.mjs` + `test/monthly-brand-sales.test.mjs` | **구현 중 — 정책 상충 가능성 있음** | `buildResolver`/`candidateFromProductName`/`matchKey`(Resolver F 자체 판정 로직) 삭제, `import { resolveIdentity } from "./unified-identity-resolver.mjs"` 추가 확인(`scripts/monthly-brand-sales.mjs:1`). 아래 6번 항목 참고 — `ROADMAP_BACKLOG.md` BACKLOG-004와 상충. |
| `scripts/unified-identity-resolver.mjs`(미추적), `scripts/shadow-compare-*.mjs`(미추적), `test/unified-identity-resolver.test.mjs`(미추적) | **완료됐지만 미커밋** | mtime 08-07 20:52~20:54, `docs/PROJECT_MEMORY.md` 6번 항목의 스펙과 함수 시그니처(`resolveIdentity`/`loadResolverContext`) 일치. 관련 테스트 13개 전부 PASS(아래 7번). |
| `scripts/build-brand-master-merge-plan.mjs`(+14/-2), `build-brand-sourcing-review-table.mjs`(+2/-1), `validate-brand-sourcing-decisions.mjs`(+2/-1) | **소폭 구현 중** | 이미 커밋된 84791b8/4f6f827 위에 얹힌 작은 후속 수정(diff 크기 10줄 내외). 어떤 동작 변경인지는 diff 내용만으로는 불명확 — **확인 필요**. |
| `config/master-data-candidates.json`, `config/master-data-schema.json`, `scripts/master-data-phase1.mjs`(전부 미추적) | **완료됐지만 미커밋(테스트로 뒷받침)** | mtime 07-29, `test/master-data-phase1.test.mjs`/`test/master-approval-workflow.test.mjs`(둘 다 미추적)와 세트. 7개 테스트 전부 PASS. |
| `test/csv-formula-injection.test.mjs`(미추적) | **테스트/검증 중(보안 회귀 가드)** | CSV 수식 인젝션 방지 테스트, 2개 PASS. 대응하는 프로덕션 코드가 어느 스크립트에 있는지는 이번 진단에서 추적하지 않음 — **확인 필요**. |
| `test/brand-intelligence-live-data.test.mjs`, `test/brand-intelligence-ui-restoration.test.mjs`(둘 다 미추적) | **테스트/검증 중(STEP67 회귀 가드)** | STEP67-7~67-8D의 "가짜 값 재도입 금지"/"57개 UI 보존" 계약을 코드로 고정한 테스트. 2개 PASS. |
| `work/monthly/2026-07.json` | **자동 생성/산출물** | mtime 08-05 13:35, `79c4302`(automate offline sales refresh workflow) 직후 재계산된 월간 아카이브. 소스가 아니라 산출물. |
| `.gitignore`(+3줄: `input/`, `.code-review-graph/`) | **개발환경/Code Review Graph 관련** | diff 확인 완료 — `code-review-graph` MCP 자체가 추가한 무시 규칙. |
| `.claude/`, `.codebuddy/`, `.cursorrules`, `.gemini/`, `.github/`, `.kiro/`, `.mcp.json`, `.qoder/`, `.windsurfrules`, `AGENTS.md`, `CLAUDE.md`, `CODEBUDDY.md`, `GEMINI.md`, `QODER.md`, `opencode.jsonc` | **개발환경 관련** | 전부 mtime 08-05 18:07 부근, AI 에이전트/IDE 설정 파일 — 소스 코드 아님. |
| `docs/DECISIONS.md`, `docs/PROJECT_MEMORY.md`, `docs/ROADMAP.md`, `docs/ROADMAP_BACKLOG.md`, `docs/DESIGN_SYSTEM.md`, `docs/DAILY_LOG/2026-08-07.md`, `docs/DAILY_LOG/2026-08-08.md` | **완료됐지만 미커밋(문서)** | mtime 08-07~08-08, 내용 완결. 단 0번 항목대로 STEP67 이후 내용은 반영 안 됨 — **문서 자체는 완결이지만 최신성은 낮음**. |

---

## 4. Brand Intelligence UI 복구 작업의 현재 상태

- **작업 방식(사용자 확인 필요 없이 코드로 확인됨)**: 새 디자인 재설계가 아니라, STEP66에서
  UI/UX Rebuild 이후 진행된 STEP67-6/7/7B/8의 "Live Data 연결 + Placeholder Purge" 과정에서
  **의도치 않게 숨겨지거나 제거된 기존(Claude 승인) UI 요소를 원상 복구하는 방향**으로
  진행됐다. `STEP67-8A-UI-AUDIT.md`가 57개 요소를 전수 비교해 28개 복구 대상을 특정했고,
  `STEP67-8B`/`STEP67-8D`가 그 28개(+STEP67-8C가 재검증한 7개 추가분) 전부를 **가짜 값을
  다시 넣지 않고 구조/셸만 복구**했다(각 보고서의 "Fabricated business values: 0" 명시).
- **현재 상태**: 정적(코드/DOM) 기준으로는 57개 요소 전부 PRESENT 상태(MISSING 0개). 실제
  브라우저에서 시각적으로 100% 원상 복구됐는지는 **STEP67-8B/8D 둘 다 스스로 "USER CHROME QA
  REQUIRED"로 명시** — 아직 확인되지 않았다.
- **미해결 곁가지 버그**: Brand Selector flicker(3번 항목) — 근본 원인은 selector 자체가
  아니라 selector trigger의 텍스트 길이에 따른 `width` 즉시 스냅으로 특정됐고, CSS `min-width`
  1줄로 수정했지만 Chrome 재현 확인 전이다.

---

## 5. Master Data / Unified Identity Resolver 관련 현재 상태

**두 개의 구분되는 트랙**(`docs/ROADMAP.md` 장기 계획 항목에 "Identity Pipeline과 별개 트랙"이라고
명시돼 있음):

1. **Master Data 트랙(브랜드/상품 마스터 승인 워크플로)**: 핵심 골격(`identity_master`/
   `brand_master`/`product_master`/`merge_history` 4-엔터티 스키마, review→approved→merged→
   rejected 전이, CSV 인젝션 방지)은 **이미 git에 커밋됨**(`84791b8`, `4f6f827`). 지금
   working tree에는 `build-brand-master-merge-plan.mjs`(+14/-2) 등 소폭 후속 수정만 남아있다
   — 무엇을 고치는 수정인지는 **확인 필요**(diff가 작아 커밋 메시지 없이는 의도 불명).
2. **Unified Identity Resolver 트랙(`scripts/unified-identity-resolver.mjs`)**: `docs/
   PROJECT_MEMORY.md` 6번 항목이 기술한 스펙과 정확히 일치하는 구현(`resolveIdentity`/
   `loadResolverContext`, Priority 1~3, Confidence Contract)이 **git에 전혀 커밋되지 않은
   채** 존재한다(미추적, mtime 08-07). 이 파일은 이제 `scripts/monthly-brand-sales.mjs`
   (docs가 "Resolver F, 범위 밖"이라고 부르는 바로 그 모듈)에서 **직접 import되어 브랜드
   판정을 위임받고** 있다(`scripts/monthly-brand-sales.mjs:1,81`) — `work/reports/
   STEP67-6.md`가 이를 "STEP67-3의 Unified Identity"라고 명시적으로 언급한다.

   **정책 상충(중요, 확인 필요)**: `docs/ROADMAP_BACKLOG.md` BACKLOG-004(마지막 갱신
   2026-08-08)는 "Monthly Report(Resolver F) 마이그레이션은 의도적으로 범위 밖 — 별도 STEP
   으로 명시 요청받아야 한다"고 기록돼 있다. 그러나 실제 uncommitted 코드는 **이미 마이그레이션을
   완료한 상태**다. 이것이 (a) 사용자가 별도로 명시 승인한 STEP67-3의 결과이고 단지 docs만
   갱신이 안 된 것인지, (b) 승인 없이 진행된 범위 이탈인지는 이번 진단(코드/git/docs 교차
   확인)만으로는 **확정할 수 없다 — 확인 필요**.

---

## 6. 현재 테스트 파일들이 무엇을 검증하는지

`node --test test/*.mjs` **직접 재실행 결과: 246/246 PASS, 0 fail**(STEP67-8D 보고서가
기록한 246/246과 정확히 일치 — 마지막 저장 시점 이후 아무것도 바뀌지 않았음을 재확인).

| 테스트 파일 | 검증 대상 |
|---|---|
| `test/unified-identity-resolver.test.mjs` | Priority 1(Product Registry VERIFIED)→2a(브랜드명 파싱)→2b(온라인 카탈로그 2차 조회)→3(Reviewed Alias) 우선순위, Confidence Contract, `loadResolverContext()`가 `brand-master.json`을 읽기만 하고 쓰지 않는지 |
| `test/monthly-brand-sales.test.mjs` | 과거월 재계산/아카이브 lock/STALE 처리, `mergeOfflineBrandSales` 정합성 |
| `test/master-data-phase1.test.mjs` | Phase 1 후보가 read-only이며 자동 승인되지 않는지 |
| `test/master-approval-workflow.test.mjs` | review→approved→merged 이력 불변성, 승인 스냅샷 동결, merge 유효성 검증 |
| `test/brand-master-merge-plan-status.test.mjs` | merge plan 상태(PENDING/APPLIED)가 재정렬/재생성에도 결정론적으로 보존되는지 |
| `test/csv-formula-injection.test.mjs` | CSV 내보내기 시 수식 인젝션(`=`,`+`,`-`,`@` 시작 셀) 방어 |
| `test/brand-intelligence-live-data.test.mjs` | Brand Intelligence가 canonical 상품명/실 ECOUNT 재고를 쓰는지, 하드코딩 SKU/고객 배열이 비어있는지 |
| `test/brand-intelligence-ui-restoration.test.mjs` | STEP67-8B가 복구한 UI 셸(Compare 도넛, Category 토글 등)이 유지되고 STEP67-8D에서 반려된 변형(pending-card 등)이 되돌아오지 않는지, 특정 금액 하드코딩 문자열이 없는지 |

---

## 7. 지금 상태에서 절대 잃으면 안 되는 변경

1. `outputs/samplas-marketing-os.{html,js,css}` + `server.mjs` + `intelligence-service.mjs`
   — STEP64~STEP67-8D 전체(Promotion Intelligence, Design System, Brand Intelligence Data
   Completion, UI 57개 요소 복구, Selector flicker 수정). git 이력에 대응하는 커밋이 하나도
   없어 **디스크에서 사라지면 전부 재작업**해야 한다.
2. `scripts/unified-identity-resolver.mjs` + `scripts/shadow-compare-*.mjs` — 미추적 파일이라
   `git stash`/`git clean` 등 어떤 실수에도 git이 전혀 보호해주지 못한다.
3. `test/*.mjs` 중 미추적 7개 — 위 코드의 회귀 가드. 코드만 있고 테스트가 사라지면 향후
   세션이 "가짜 값 재도입 금지" 계약을 검증할 방법이 없어진다.
4. `docs/DAILY_LOG/2026-08-07.md`, `2026-08-08.md`, `PROJECT_MEMORY.md`, `ROADMAP.md`,
   `ROADMAP_BACKLOG.md`, `DECISIONS.md` — 미추적이지만 STEP63 시리즈 전체의 유일한 기록.

---

## 8. 서로 섞여 있어서 분리해야 할 변경

- **`outputs/samplas-marketing-os.{html,js,css}` / `server.mjs` / `intelligence-service.mjs`
  단일 diff 안에 최소 4개의 서로 다른 기능 단위가 뒤섞여 있다**: (1) Promotion Intelligence
  신규 구축(STEP64-4~65-6), (2) Design System 재설계(STEP66-0~66-2), (3) Brand Intelligence
  Data Completion(STEP67-6~67-8), (4) UI 57개 요소 복구 + Selector flicker 수정
  (STEP67-8A~8D). 지금 이대로 커밋하면 하나의 거대 커밋에 4개 STEP이 뭉쳐 향후 특정 STEP만
  되돌리기가 불가능해진다 — 커밋 시점에는 반드시 STEP 단위 분리를 고려해야 한다.
- **`scripts/monthly-brand-sales.mjs`**: Resolver F 내부 로직 삭제 + Unified Identity
  위임이라는 **정책적으로 중대한 변경**이 다른 소폭 Master Data 스크립트 수정과 같은
  "tracked modified" 묶음 안에 있다 — 커밋 시 반드시 별도 커밋으로 분리하고, `docs/
  ROADMAP_BACKLOG.md` BACKLOG-004 갱신이 함께 이뤄져야 한다.
- **Master Data 트랙 vs Identity Pipeline 트랙**: `docs/ROADMAP.md`가 이미 "별개 트랙"이라고
  명시했음에도, 지금 `scripts/monthly-brand-sales.mjs`를 통해 두 트랙이 실제로 얽히기
  시작했다 — 두 트랙의 경계가 더 이상 문서와 일치하지 않는다.

---

## 9. 다음에 진행해야 할 가장 안전한 STEP

**코드 수정 없음. 사용자가 실제 Chrome에서 Brand Intelligence(`#BrandDashboard`)를 열어
브랜드를 변경했을 때 STEP67-8D의 `min-width: 206.5625px;` 수정으로 selector flicker가
실제로 사라졌는지만 확인한다.**

근거:
- 이것이 working tree의 가장 마지막 변경이며, 유일하게 "코드는 끝났지만 검증만 안 된" 상태다.
- 수정 범위가 CSS 1줄뿐이라 리스크가 가장 작다(`STEP67-8D-BRAND-SELECTOR-FLICKER-MINIMUM-FIX.md`
  5번 항목이 다른 영역 비침범을 이미 diff로 증명).
- 이 확인이 끝나야 STEP67-8D 전체(57개 UI 복구)와 STEP67-6~67-8 Data Completion까지 연쇄적으로
  "READY_FOR_USER_QA" → "PASS"로 확정할 수 있고, 그래야 비로소 무엇을 커밋할지(8번 항목의 분리
  포함) 판단할 수 있다.
- 코드/데이터를 전혀 건드리지 않으므로 이번 진단의 "코드 미수정" 제약과도 완전히 부합한다.

---

## 10. 확인 필요 항목 (추측하지 않고 명시)

1. `scripts/build-brand-master-merge-plan.mjs`(+14/-2)/`build-brand-sourcing-review-table.mjs`
   (+2/-1)/`validate-brand-sourcing-decisions.mjs`(+2/-1)의 정확한 변경 의도 — diff가 작아
   커밋 메시지 없이는 불명확.
2. `scripts/monthly-brand-sales.mjs`의 Unified Identity 위임이 사용자가 명시 승인한 별도
   STEP(STEP67-3)의 산출물인지, 승인 없이 BACKLOG-004 범위를 벗어난 것인지.
3. STEP67-1/67-2/67-3/67-4/67-5의 실제 작업 내용 — 대응하는 `work/reports/STEP67-{1..5}*.md`
   파일이 존재하지 않는다(STEP67-6~8D 본문에서 간접 언급되는 내용으로만 재구성 가능).
4. `test/csv-formula-injection.test.mjs`가 검증하는 실제 프로덕션 코드 위치.
5. STEP67-8D의 CSS `min-width` 수정이 실제 Chrome에서 flicker를 해결했는지(9번 항목의 다음
   STEP 그 자체).

---

## 부록 — Code Review Graph 진단 결과

- `list_graph_stats_tool`: Nodes 2196 / Edges 22559 / Files 73(전부 javascript) / 마지막 빌드
  `2026-08-10T01:20:34`, HEAD `4f6f827`과 일치(`head_matches_build: true`).
- `detect_changes_tool`(HEAD 기준): 변경 파일 14개, 함수/클래스 201개 영향, **Overall risk
  score 0.85(High)**, 테스트 갭 198개. 미검증 함수 예시: `handleIntelligenceRequest`,
  `routeIntelligenceRequest`, `json`, `isGiftSalesLine`, `buildClientsOverview`.
- **그래프 커버리지 공백(확인됨)**: `scripts/unified-identity-resolver.mjs`는
  `query_graph_tool`(`file_summary`)/`semantic_search_nodes_tool` 양쪽 모두에서 검색되지
  않는다 — git에 추적되지 않는 파일은 그래프 인덱싱 대상(73개 파일)에서 빠져 있다. 즉
  Unified Identity Resolver 트랙 전체가 지금 code-review-graph의 사각지대에 있다 — 이 파일들을
  `git add`하기 전까지는 그래프 기반 리뷰/영향도 분석이 이 트랙을 다루지 못한다.

---

**작성 방식**: 코드/설정/데이터 파일 수정 없음. 이 문서(신규 생성) 외 어떤 파일도 변경하지
않았다. `node --test`는 읽기 전용 실행이며 파일을 남기지 않는다.

---

## 추가 기록 — 같은 날 이어진 작업(진단 이후)

이 진단 이후 같은 세션에서 실제로 진행된 작업(코드 수정 포함) 요약만 남긴다 —
상세는 각 `work/reports/STEP67-8D-*.md`/`STEP67-9-*.md` 참고.

- **STEP67-8D 계열(Comparison Selector Flicker → Root Cause → Brand A Local
  Selector)**: 비교 모드에서 "기준 브랜드" 클릭 시 화면이 튀는 문제를 3단계로
  진단·수정했다. 최종적으로 `outputs/samplas-marketing-os.js`의 outside-click
  가드 로직 수정(root cause fix) + `entitySelectorInstances`에 `compareA`
  인스턴스를 신규 등록해 "기준 브랜드"가 상단 Primary Selector로 리다이렉트되지
  않고 자체 로컬 드롭다운을 갖도록 재구성(Brand B와 대칭 구조,
  `outputs/samplas-marketing-os.html`/`.js` 수정). **사용자 실제 Chrome QA로
  PASS 확인 완료** — Comparison Brand A/B selector 정상, flicker 해결, Brand A
  선택 시 메인 Brand Intelligence 브랜드 동기화 정상.
- **STEP67-9 — Brand Intelligence Live Data Connection Architecture Review
  시작**: UI 복구 트랙을 마감하고 실데이터 연결 트랙으로 전환. 코드 수정 없이
  `code-review-graph`+실제 코드 조사만 수행 — 매출/판매수량/주문수/객단가/
  현재 재고/System Status는 이미 canonical source에 연결돼 있음을 재확인,
  Sell-through/브랜드 메타데이터(지역·카테고리·업데이트)/Comparison
  실데이터는 여전히 source 부재로 BLOCKED임을 확정. 상세: `work/reports/
  STEP67-9-BRAND-INTELLIGENCE-LIVE-DATA-ARCHITECTURE.md`.
- **STEP67-9E-1/9E-2 — Brand Comparison 핵심 KPI + 비교 대상 기간 데이터 연결**:
  Period Performance(매출/판매수량/주문수/객단가) Brand A/B × 현재 기간을
  기존 `/api/reports/monthly` 그대로 재사용해 연결(API↔UI 실측 일치 확인,
  사용자 Chrome QA PASS). 이어서 비교 대상 기간(target period) state까지
  준비(`entityCompareTargetPeriodData`, 당시엔 미노출 state로만 존재).
- **STEP67-9F-0 — Brand Comparison UI Polish Spec**: 코드 없이 "행=브랜드,
  열=현재/비교 대상/증감" 표 구조를 명세만 확정(구현 아님).
- **STEP67-9G-0/1A/1B — Period Performance 레이아웃 진단·구현**: 비교 모드
  KPI 카드 안에 4열 표를 억지로 넣었더니 카드 폭(592px) vs 표 최소폭(960px)
  충돌로 옆 카드 위에 겹쳐 보이는 문제를 실측 진단(`getBoundingClientRect`) →
  Period Performance 4개를 `#entityHeroKpiGrid` 밖 독립 full-width 섹션으로
  분리해 해결(카드 폭 1196px, 표 폭 1166px로 완전히 들어감). 사용자 실제
  Chrome QA PASS. 상세: `work/reports/STEP67-9G-1A-*.md`/`STEP67-9G-1B-*.md`.
- **STEP67-9H-0/9H-2 — Customer Composition Comparison**: 먼저 코드 진단
  (`buildBrandCustomerComposition()` 등, 새 API/새 resolver 불필요 확인,
  API error와 실제 0건이 구분되지 않던 기존 사각지대 발견) → Comparison
  Mode 전용으로 Customer Composition을 Brand A/B 완전 동등 50:50 column(큰
  도넛 150px + 유형별 legend, 기존 `.brand-customer-donut`/색상 토큰 그대로
  재사용)으로 재구성하고, 단일 브랜드용 큰 도넛+TOP5+AI Insight는 비교 모드에서
  중복 노출되지 않도록 숨김. 진단 중 발견한 "브랜드 데이터가 아예 없을 때
  0%로 채운 legend가 뜨는" 문제를 구현 중 자체적으로 발견·수정(그 경우
  legend를 비우고 도넛의 정직한 "데이터 없음" 문구만 남김). 자동 테스트
  246/246 PASS, Chrome 자동 검증 완료 — **사용자 수동 Chrome QA 아직 필요**.
  상세: `work/reports/STEP67-9H-0-*.md`, 본 문서 하단 STEP67-9H-2 섹션.

---

## STEP67-9H-2 — Customer Composition Comparison Layout(상세)

### Root Cause / UX Problem

Comparison Mode 진입 시 Customer Composition 영역에 (1) Brand A/B의 작은
비교 도넛(STEP67-9H-1, 72px, 실데이터 연결됨)과 (2) 그 아래 단일 브랜드용
큰 도넛(150px)+TOP5+AI Insight가 **동시에** 노출되어, 같은 브랜드(Brand A)
정보가 두 군데(작은 도넛 + 큰 도넛)에 중복 표시되고 화면 스캔 흐름이
끊기는 문제가 있었다. 근본 원인은 STEP67-9H-1이 "기존 mini donut shell에
데이터만 연결"하는 최소 범위로 구현되면서, 그 아래 단일 브랜드 큰 도넛
섹션을 비교 모드에서 숨기는 처리를 하지 않은 채 남겨 둔 것.

### Files Changed

- `outputs/samplas-marketing-os.html`
- `outputs/samplas-marketing-os.css`
- `outputs/samplas-marketing-os.js`

(server.mjs/intelligence-service.mjs/test/API/resolver: 변경 없음)

### Implementation

1. **HTML**: `#entityCompositionCompareBlock`의 두 column을 각각
   `.brand-customer-donut-card`(기존 카드 chrome 재사용)로 감싸고, 브랜드명
   `<p class="entity-compare-composition-brand-name">`(신규 클래스, column
   header 역할 — "기준"/"비교"/A·B 표기 없음)을 헤더로, 도넛은 기존
   `#entityCompareCompositionDonutA/B`(id 유지, JS 함수 시그니처 무변경) 아래
   신규 `#entityCompareCompositionLegendA/B`(`<ul class="clients-donut-legend
   brand-customer-legend">`, 단일 브랜드 legend와 동일 클래스 재사용) 추가.
   AI Insight `<article>`에 `id="entityCompositionInsightCard"`를 새로 부여해
   다른 5곳의 동일 클래스(`brand-customer-insight-card`, Commerce/Category/
   Client Workspace/Promotion 등)와 안전하게 구분(scope 사고 방지).
2. **CSS**: `body.entity-compare-on .entity-compare-composition`을 기존
   `flex` 좌우 나열에서 `display:grid; grid-template-columns: repeat(2,
   minmax(0,1fr))`로 변경(완전 동등 50:50). `.entity-compare-mini-donut`
   크기를 72px→150px로 확대(단일 브랜드 큰 도넛과 동일 크기).
   `#entityCompositionContent > .brand-customer-grid`와
   `#entityCompositionInsightCard`를 비교 모드에서 `display:none`(단,
   `.brand-customer-grid`는 Category Intelligence와 클래스를 공유하므로
   `#entityCompositionContent` 자식으로 selector를 좁혀 Category에 영향이
   가지 않도록 함 — 구현 중 이 공유 클래스를 직접 확인하고 범위를 좁혔다).
   `≤640px` 반응형 규칙도 기존 `flex-direction:column`(더 이상 유효하지
   않음)에서 `grid-template-columns:1fr`로 함께 갱신.
   **버그 수정**: 처음 `.entity-compare-mini-label` 기본 CSS를 실수로
   삭제했다가 Category Intelligence의 "기준 · 브랜드명 1위" 라벨이 그
   클래스를 공유해서 쓰고 있음을 grep으로 확인하고 즉시 복구했다(코드
   반영 전 자체 발견·수정, Category Intelligence 실측 영향 없음 확인).
3. **JS**: `renderEntityCompareComposition()`이 기존에 공용
   `#entityCompositionLegend`(비교 모드에서 이제 숨겨짐)에 색상 스와치만
   쓰던 것을 제거하고, 신규 `renderEntityCompareCompositionLegend()`로
   교체 — Brand A/B `stats`의 합집합 유형 목록을 같은 순서로 두 legend에
   모두 렌더링해(한쪽에만 있는 유형은 0%로 표시) 두 column의 행 수가
   달라 레이아웃이 흔들리지 않게 했다. **구현 중 자체 발견·수정**: 처음
   버전은 브랜드 상태가 `status !== "ready"`(미선택/에러/해당 기간 데이터
   없음)여도 0%로 채운 legend를 그대로 보여줘, "측정했더니 0%"처럼 오인될
   여지가 있었다 — Chrome으로 직접 확인(BONNAE 선택 시 도넛은 정직하게
   "해당 기간 오프라인 고객 데이터 없음"인데 legend는 스타일리스트
   0%/일반 손님 0%로 채워짐)한 뒤, `status !== "ready"`일 때 해당 legend를
   완전히 비우도록 수정했다.

### Data Source

기존 그대로: `GET /api/brand-intelligence/:brandCode/customer-composition?month=`
→ `buildBrandCustomerComposition()`(server.mjs, 미변경) → 프론트
`entityCompareCompositionState.a/b`(STEP67-9H-1, 미변경) → 이번 STEP은 이
state를 **가져오는 방식은 그대로 두고 보여주는 방식만** 바꿨다. 새 계산/새
분류/새 API/새 resolver 없음.

### Comparison OFF Regression

- `.brand-customer-grid`(큰 도넛+TOP5) `display:grid`(정상 노출),
  `#entityCompositionInsightCard` `display:grid`(정상 노출),
  `#entityCompositionCompareBlock` `display:none`(정상 숨김).
- 큰 도넛 센터 라벨 "27건"(CARNET ARCHIVE 실측값과 일치), legend/TOP5 기존과
  동일하게 렌더링됨을 Chrome에서 직접 확인(스크린샷 대조).

### Comparison ON Result

Chrome 실측(CARNET ARCHIVE vs TROUBLED WATERS, 2026년 8월):

- `#entityCompositionCompareBlock` `display:grid`,
  `grid-template-columns: "466px 466px"`(완전 동등 50:50 확인).
- Column 헤더: "CARNET ARCHIVE"/"TROUBLED WATERS"(실제 브랜드명, A/B 표기
  없음).
- 도넛 센터: 27건/16건(각각 실제 오프라인 구매 건수, STEP67-6 보고서의
  실측값과 일치).
- Legend(union 4개 유형, 두 column 동일 순서):
  - CARNET ARCHIVE: 스타일리스트 93%/일반 손님 7%/외국인 0%/직원 구매 0%
  - TROUBLED WATERS: 스타일리스트 88%/일반 손님 0%/외국인 6%/직원 구매 6%
  - 동일 유형(예: 스타일리스트)이 두 column에서 동일한 색(entityCompositionColors,
    브랜드별 분기 없음) 사용을 코드 구조상 확인.
- `.brand-customer-grid`/`#entityCompositionInsightCard` 둘 다
  `display:none`(단일 브랜드 큰 도넛/TOP5/AI Insight 중복 노출 제거 확인).
- Category Intelligence 섹션은 스크린샷에서 정상적으로 그대로 노출됨(영향
  없음 육안 확인).

### Tests

- `node --check outputs/samplas-marketing-os.js` → PASS
- `git diff --check`(html/css/js) → PASS
- `node --test test/*.mjs`(전체) → **246/246 PASS**
- `node --test test/brand-intelligence-live-data.test.mjs
  test/brand-intelligence-ui-restoration.test.mjs` → **2/2 PASS**

### Chrome QA

**직접 수행함(자동화, 이 세션의 Chrome 도구로 실제 로컬 서버 렌더링을
관찰)** — 위 "Comparison OFF Regression"/"Comparison ON Result" 전부 실제
DOM 조회 + 스크린샷 대조로 확인했다(추정 아님). 다만:

- **Interaction 항목 중 "primary brand 변경 시 왼쪽 데이터 정상 갱신"**:
  Brand A를 바꾸는 직접 테스트는 이번 STEP에서 별도로 재확인하지 않았다
  (STEP67-9G-1B에서 이미 같은 트리거 경로로 검증된 바 있으나, Customer
  Composition의 새 legend가 그 트리거를 타는지는 코드상으로는 확인했지만
  Chrome 실측은 생략함) — **확인 필요**로 남긴다.
- **Responsive/좁은 viewport 실측**: 이 세션의 `resize_window` 도구가 이
  탭의 실제 렌더링 viewport 크기를 바꾸지 못했다(`innerWidth` 불변 확인,
  환경 제약으로 판단). `@media (max-width: 640px)` 규칙을 grid 1열로
  갱신하는 코드 수정은 했으나, **실제 좁은 화면에서의 렌더링은 육안으로
  확인하지 못했다** — PASS로 단정하지 않는다, 사용자 수동 확인 필요.
- 이 외 항목(Comparison OFF/ON 핵심 구조, 브랜드명, 도넛, legend, 색상,
  중복 제거, count/sales 토글)은 전부 직접 관찰로 확인됨.

### Remaining Risk

- 위 "Chrome QA" 항목에 명시한 2가지 미확인 사항(Brand A 변경 시 왼쪽 갱신
  직접 재확인, 실제 좁은 화면 렌더링).
- Customer Composition 전용 legend에는 기존 단일 브랜드 legend가 갖고 있던
  hover-하이라이트(legend↔도넛 상호 강조) 인터랙션을 추가하지 않았다(요구
  사항에 없었고 범위 확장을 피하기 위해 의도적으로 생략) — 필요하면 별도
  STEP.
- STEP67-9H-0이 발견한 "API error vs 실제 0건 미구분" 사각지대는 이번
  STEP에서 legend 레벨(status 기반)로는 개선했으나, 서버 응답 자체
  (`buildBrandCustomerComposition`)의 구분은 여전히 없다 — 백엔드 변경은
  이번 STEP 범위 밖이라 그대로 남아 있다.

### git diff --stat

```
 outputs/samplas-marketing-os.css  | 3283 ++++++++++++++++++++++++++-
 outputs/samplas-marketing-os.html |  929 +++++++-
 outputs/samplas-marketing-os.js   | 4389 ++++++++++++++++++++++++++++++++++++-
 3 files changed, 8501 insertions(+), 100 deletions(-)
```

(이 세션 전체의 누적 diff — HEAD `4f6f827` 이후 지금까지 건드린 모든 STEP의
합. 이번 STEP67-9H-2만의 순수 diff는 위 세 파일 중 Customer Composition
관련 블록에 한정되며, server.mjs/intelligence-service.mjs/test/기타 파일은
`git status --short`상 이번 STEP 전후로 전혀 추가되지 않음.)
