# DAILY_LOG — 2026-08-07

## 오늘 완료한 STEP

- **STEP63-3 (PASS)** — Clients Identity Pipeline Integration. `intelligence-service.mjs`
  의 `buildClientsOverview()`가 offline `purchaseDetails`에 canonical 브랜드 필드
  4개(`canonicalBrandCode`/`canonicalBrandName`/`brandConfidence`/
  `operationalBrandGroup`)를 원본 `brand` 필드를 절대 덮어쓰지 않고 추가하도록 수정.
  프론트엔드는 `clientsTimelineBrandDisplay()` 함수로 Timeline Card/Order Detail
  Drawer의 브랜드 표시를 통일. Confidence Contract에 따라 CANDIDATE 확신도는 화면에
  표시되지 않음(데이터 계층은 정확, 표시 계층은 보수적). Clients Revenue Diff 0원,
  244/244 테스트 통과.
- **STEP63-3A (PASS)** — Online Clients Regression Audit. "온라인 고객 일반"의
  "브랜드 정보 없음"/"제품 정보 없음" 표시가 STEP63-3 이전(git HEAD 시점)부터 이미
  있던 사전 존재 동작임을 git diff로 증명. Regression 아님.
- **STEP63-3B-0 (조사, NO-GO)** — Personal Payment Bridge Feasibility Audit. Cafe24
  개인결제창 주문과 ECOUNT 판매전표를 잇는 신뢰 가능한 키가 없음을 실제 6개 고객
  사례(권순환/전예린/이지유/최재은/김수지 등)로 확인. 날짜 오프셋 0~8일, 금액 검증
  불가(ECOUNT 개인결제 라인은 salesAmount 항상 null), 대리결제로 인한 이름 불일치
  사례까지 발견.
- **STEP63-3B-1 (조사, NO-GO)** — ECOUNT Slip Architecture Audit. 3B-0과 다른 각도
  (아키텍처 관점)에서 조사한 결과, ECOUNT 원천 자체가 Slip Header(전표 헤더) 개념
  없이 Line 단위 Excel export("판매현황" 시트) 하나만 제공한다는 사실을 확인 — 두
  독립적 조사가 같은 NO-GO 결론에 도달.
- **STEP63-4 (PASS)** — Brand Dashboard Identity Pipeline Migration. `server.mjs`의
  `aggregateCafe24BrandSalesByBrandCode()`에 `resolveOnlineProductBrandCode()` 보조
  함수 추가(45줄 변경). Cafe24 자체 brand_code가 실패했을 때만 Product Registry
  Priority 1(VERIFIED만)으로 보조 조회. 2026-07 데이터는 UNASSIGNED가 이미 0건이라
  실측 Brand Coverage는 SAME이었으나, 메커니즘 검증 완료. Revenue/Quantity Diff 0.
- **STEP63-5 (PASS)** — Commerce Identity Pipeline Migration. 조사 결과 Commerce는
  독자적인 브랜드/상품 판정 로직이 전혀 없고 STEP63-4가 이미 마이그레이션한
  `/api/diagnostics/brand-sales`를 그대로 소비할 뿐임을 확인 — **코드 변경 없이
  검증만으로 PASS**.
- **STEP63-6 (STOPPED)** — Inventory Identity Pipeline Migration. Inventory가 Brand
  Master(SoT)가 아닌 별도 레거시 파일(`work/intelligence/brand-master-list.json`,
  273개 브랜드)을 쓰고 있으며 SKU의 52%(5,213/10,000)가 canonical 미해결(BONNAE
  포함)임을 발견. 사용자에게 우선순위(Pipeline 우선 vs 완전 보수적)를 질의했고,
  사용자가 "SKU 정확성 최우선, 가능성 있으면 중단"을 지시해 구현을 STOPPED로 종료.
- **Documentation Foundation** — `docs/PROJECT_MEMORY.md`, `docs/ROADMAP.md`,
  `docs/ROADMAP_BACKLOG.md`, `docs/DECISIONS.md`, `docs/DAILY_LOG/2026-08-07.md`
  최초 생성. Claude/Codex/ChatGPT 공통 참조 문서 시스템 구축.

## 오늘 결정사항

- **Decision-004**: Identity Pipeline은 "기존 정확한 경로가 실패했을 때만" 보조로
  작동하도록 설계한다(Brand Dashboard 기준 확정, Decision 로그 참고).
- **Decision-005**: Inventory는 SKU 단위 정확성을 브랜드 Identity 개선보다 우선한다
  — 사용자 직접 지시로 확정. Inventory Migration은 이 원칙이 만족될 때까지 보류.
- **Decision-006**: Personal Payment Bridge는 코드로 풀지 않는다(NO-GO 확정, 재검토는
  운영 프로세스 변경이 선행돼야 함).
- **Decision-009**: 이미 공유 엔드포인트를 쓰는 화면(Commerce)은 새 Identity 로직을
  중복 구현하지 않는다 — 상위 화면(Brand Dashboard) 마이그레이션이 자동으로
  전파된다는 것을 확인하고 그대로 인정.

## 새롭게 발견한 사실

1. **Inventory는 Brand Master SoT를 전혀 쓰지 않는다** — 별도 레거시 파일
   (`work/intelligence/brand-master-list.json`, 273개 브랜드 vs `work/brand-master.json`
   의 291개 — 이미 드리프트 발생 중)을 쓴다. 이는 STEP62-5가 처음 문서화한
   "Resolver A~G" 목록에서 Resolver B로 이미 알려져 있었지만, 그 실제 데이터 소스가
   Brand Master가 아니라는 사실은 이번 STEP63-6에서 처음 실측 확인했다.
2. Inventory 현재 브랜드 커버리지 실측: 10,000개 SKU 중 5,213개(52.1%)가 원문 텍스트
   그대로 표시됨(비canonical). Critical Brand 10개 중 4개(OURSELVES REMAKE/424/
   COZY WORLDWIDE/4FEX)는 이미 canonical, 3개(BONNAE/RACER WORLDWIDE/KAMIGIN)는
   비canonical, 나머지(SUNDAY OFF CLUB/QQQ/POP CO)는 현재 재고 스냅샷에 매칭 SKU가
   아예 없거나(N/A) 정책상 애초에 canonical이 아님(정상).
3. Commerce는 백엔드 코드베이스 전체를 뒤져도 독립적인 브랜드/상품 판정 로직이
   없다 — `/api/diagnostics/brand-sales`(Brand Dashboard와 동일 엔드포인트)만 쓴다.
   이 사실 덕분에 STEP63-5가 코드 변경 없이 종료됐다.
4. Personal Payment 실측 사례에서 Cafe24 `billing_name`(계정 소유자명)과 실제 상품명
   메모에 적힌 인물명이 다른 "대리결제" 케이스를 발견(이지유 계정으로 "박지연/박상욱"
   결제) — 이름 매칭조차 신뢰할 수 없는 추가 근거.
5. Cafe24 개인결제창 payment_amount 집계(월 단위)와 ECOUNT 개인결제 라인 그룹 수가
   집계 레벨에서도 일치하지 않음(2026-04: Cafe24 16건 vs ECOUNT 13개 그룹) — 개별
   사례 문제가 아니라 시스템적 불일치.

## Backlog 변경

- BACKLOG-001(Inventory Migration 재개) 신규 등록.
- BACKLOG-002(Clients Confidence Contract 정책 결정) 신규 등록(이미 STEP63-3
  보고서에 있던 옵션을 백로그로 정식 이관).
- BACKLOG-003(Personal Payment Bridge) 신규 등록.
- BACKLOG-004(Monthly Report 미마이그레이션) 신규 등록(Technical Debt로 기록만).
- BACKLOG-005(Brand Master 이중 저장소) 신규 등록 — BACKLOG-001과 근본 원인 공유.

## 내일 첫 작업

`ROADMAP.md`의 "다음 STEP" 후보 중 사용자와 함께 우선순위를 정한다. 특히:

1. BACKLOG-001(Inventory)과 BACKLOG-005(Brand Master 이중 저장소)를 묶어서 처리할지
   결정 — 코드 마이그레이션보다 데이터 마이그레이션(레거시 파일을 Brand Master로
   교체)이 더 안전한 선택지일 수 있다는 점을 우선 검토.
2. BACKLOG-002(Clients Confidence Contract)의 세 옵션 중 하나를 선택할 준비가 됐는지
   확인 — 특히 옵션 1(온라인 카탈로그 캐싱 전략)의 구현 난이도를 먼저 가볍게 조사해
   보는 것을 권장.
3. 위 결정이 나기 전까지는 새로운 STEP63-x 구현을 시작하지 않는다 — 문서 시스템
   (`docs/`)이 이제 갖춰졌으니, 다음 세션은 `PROJECT_MEMORY.md`/`ROADMAP.md`를 먼저
   읽고 시작하는 것을 표준 절차로 삼는다.
